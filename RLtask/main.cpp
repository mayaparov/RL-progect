#include <iostream>
#include "Logger.h"

int main()
{

	Logger log_file, a, b, c, d;
	report msg;
	std::cout << msg.is_emtpy();
	/*report msg("ERROR", "memory leak");
	log_file.print_msg(msg);
	std::string type, text;
	
		std::cout << "type:\t";
		std::cin >> type;
		std::cout << "message:\t";
		std::cin >> text;
		msg.set_type(type);
		msg.set_text(text);
		log_file.print_msg(msg);*/
		
	

}

